<%= turbo_stream_from @game %>

<div class="page-shell max-w-2xl">
  <h1 class="page-title text-4xl font-extrabold tracking-tight mb-4"><%= @game.quiz.title %></h1>
  <%= render "games/host_area", game: @game %>
</div>
